services:
  database:
    image: postgres:alpine
    container_name: database
    restart: always
    env_file: .env
    ports:
      - "5432:5432"
    volumes:
      - ./src/db/migrations/0000_init.sql:/docker-entrypoint-initdb.d/init.sql
